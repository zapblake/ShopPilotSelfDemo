[build]
builder = "nixpacks"
buildCommand = "pnpm exec prisma generate --schema=../../packages/db/prisma/schema.prisma"

[deploy]
startCommand = "node src/worker.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
